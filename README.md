# My Dotfiles

Dotfiles managed by [Chezmoi](https://www.chezmoi.io/).

## Setup a new machine

```bash
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply varikin
```

Or, install Chezmoi with a package manager, then run:

```bash
chezmoi init --apply varikin
```
